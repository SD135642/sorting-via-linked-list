
//returns the length of the string passed
size_t my_strlen(char *src);

//copies the string and returns a pointer to the copy
char *my_strcpy(char *dst, char *src);

//makes all the characters in the string lowercase
void my_str_lower(char *src);
